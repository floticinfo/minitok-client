Add under the current unreleased or next-release section:

- Added hardened local completed-run evidence with schema validation, sensitive-data sanitization, run-ID path-traversal protection, same-directory atomic writes, and consistent `latest.json` mirroring.
- Added focused node:test coverage for valid records, malformed input, sensitive values, unsafe destinations, filesystem errors, and latest-record consistency.
- This metadata does not perform or attest to deployment, entitlement, payment, signing, provider authorization, or Git history changes.
